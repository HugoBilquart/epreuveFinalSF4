{% extends 'base.html.twig' %}

{% block title %}New Joueurs{% endblock %}

{% block body %}
    <h1>Créer une nouvelle carte pour le joueur "{{ joueur.nom }}"</h1>

    {{ form_start(form) }}
        {{ form_errors(form) }}
        {{ form_rest(form) }}
        <button type="submit" class="btn btn-primary">Save</button>
	{{ form_end(form) }}

    <a href="{{ path('joueurs_index') }}">back to list</a>
{% endblock %}